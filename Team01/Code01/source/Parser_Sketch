IMPORTANT NOTES:
 - Line =/= Stmt
 	- Proc name declaration is not a stmt

UI calls Parser.parseText(LIST_OF_STRINGS)

parseText(LIST_OF_STRINGS)	
	For each srcString:
		parseLine(srcString, prevLineType, parentASTNodePointer)
	-> Consume Token

parseLine(srcString, prevLineType, parentASTNodePointer)
	//determine src_line type
	//Use whitespace to split string
		//Search for particular tokens that indicate line type -> can fail due to while() <- Note no space betwen while and open bracket
			//Most should be found in first token?
			//NIL: '{', '}', '} else {', 'procedure' 
			//Alt: accept both while and while(, if and if( etc while(x==3)
	//call appropriate parse function on array