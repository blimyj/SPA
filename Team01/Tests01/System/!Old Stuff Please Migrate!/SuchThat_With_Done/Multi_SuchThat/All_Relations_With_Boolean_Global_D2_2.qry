1 - Follows, Next, Next*
stmt s;
Select BOOLEAN such that Follows (1, s) and Next (1, s) and Next* (s, 5) with s.stmt# = 3
FALSE
5000
2 - Modifies, Uses
variable v;
Select BOOLEAN such that Modifies (9, v) and Uses (36, v) with v.varName = "gold"
TRUE
5000
3 - Calls, Calls*, Modifies
procedure p1, p2;
Select BOOLEAN such that Modifies (p1, "coal") and Calls ("thirdore", p1) and Calls (p1, p2) with p1.procName = "fourthore"
TRUE
5000
4 - Parent, Parent*, Next, Next*
stmt s; assign a; read r; print p; call c; if i;
Select BOOLEAN such that Parent (s, a) and Parent* (i, r) and Next (s, p) and Next* (p, c) with s.stmt# = 9
FALSE
5000
5 - Follows*, Parent*, Modifies, Next*
stmt s; assign a; variable v;
Select BOOLEAN such that Follows* (21, s) and Parent* (23, v) and Modifies (s, v) and Next* (a, s) with a.stmt# = 24
FALSE
5000
6 - Parent*, Uses, Modifies, Next, Calls*
procedure p; variable v1, v2; call c;
Select BOOLEAN such that Parent (_, c) and Uses (p, v1) and Modifies (p, v2) with c.procName = p.procName and v1.varName = v2.varName
TRUE
5000
7 - Follows*, Follows*, Next*, Next*
stmt s1, s2, s3, s4, s5;
Select BOOLEAN such that Follows* (s1, s2) and Follows* (s2, s3) and Next* (s3, s4) and Next* (s4, s5) with s3.stmt# = s4.stmt# and s5.stmt# = s1.stmt#
TRUE
5000
8 - Uses, Uses, Uses, Uses, Uses
constant c; stmt s;
Select BOOLEAN such that Uses (s, "gold") and Uses (s, "emerald") and Uses (s, "lapis") and Uses (s, "iron") and Uses (s, "coal) with s.stmt# = c.value
FALSE
5000
9 - Parent, Follows, Modifies
stmt s; while w; 
Select BOOLEAN such that Parent (w, s) and Follows (38, s) and Modifies (s, "gold") with w.procName = s.procName
TRUE
5000
10 - Parent, Parent*, Follows, Uses, Uses
procedure p; stmt s1, s2, s3; while w;
Select BOOLEAN such that Parent (s1, s2) and Parent* (s1, s3) and Follows (_, s2) and Uses (p, "redstone") and Uses (w, "coal") with w.stmt# = 18
TRUE
5000
