1 - Follows Integer

Select BOOLEAN such that Follows(1, 2)
we didn't write the result for this query
1000
1 - Follows Wildcard

Select BOOLEAN such that Follows(1, _)
we didn't write the result for this query
1000
1 - Follows Wildcard

Select BOOLEAN such that Follows(_, 1)
we didn't write the result for this query
1000
1 - Follows Wildcard

Select BOOLEAN such that Follows(_, _)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s;
Select BOOLEAN such that Follows(1, s)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s;
Select BOOLEAN such that Follows(s, 1)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s;
Select BOOLEAN such that Follows(s, s)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s;
Select BOOLEAN such that Follows(s, _)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s;
Select BOOLEAN such that Follows(_, s)
we didn't write the result for this query
1000
1 - Follows Statement
stmt s1, s2;
Select BOOLEAN such that Follows(s1, s2)
we didn't write the result for this query
1000
1 - Follows Read
read r;
Select BOOLEAN such that Follows(1, r)
we didn't write the result for this query
1000
1 - Follows Read
read r;
Select BOOLEAN such that Follows(r, 1)
we didn't write the result for this query
1000
1 - Follows Read
read r;
Select BOOLEAN such that Follows(r, r)
we didn't write the result for this query
1000
1 - Follows Read
read r;
Select BOOLEAN such that Follows(r, _)
we didn't write the result for this query
1000
1 - Follows Read
read r;
Select BOOLEAN such that Follows(_, r)
we didn't write the result for this query
1000
1 - Follows Read
read r1, r2;
Select BOOLEAN such that Follows(r1, r2)
we didn't write the result for this query
1000
1 - Follows Print
print p;
Select BOOLEAN such that Follows(1, p)
we didn't write the result for this query
1000
1 - Follows Print
print p;
Select BOOLEAN such that Follows(p, 1)
we didn't write the result for this query
1000
1 - Follows Print
print p;
Select BOOLEAN such that Follows(p, p)
we didn't write the result for this query
1000
1 - Follows Print
print p;
Select BOOLEAN such that Follows(p, _)
we didn't write the result for this query
1000
1 - Follows Print
print p;
Select BOOLEAN such that Follows(_, p)
we didn't write the result for this query
1000
1 - Follows Print
print p1, p2;
Select BOOLEAN such that Follows(p1, p2)
we didn't write the result for this query
1000
1 - Follows Call
call c;
Select BOOLEAN such that Follows(1, c)
we didn't write the result for this query
1000
1 - Follows Call
call c;
Select BOOLEAN such that Follows(c, 1)
we didn't write the result for this query
1000
1 - Follows Call
call c;
Select BOOLEAN such that Follows(c, c)
we didn't write the result for this query
1000
1 - Follows Call
call c;
Select BOOLEAN such that Follows(c, _)
we didn't write the result for this query
1000
1 - Follows Call
call c;
Select BOOLEAN such that Follows(_, c)
we didn't write the result for this query
1000
1 - Follows Call
call c1, c2;
Select BOOLEAN such that Follows(c1, c2)
we didn't write the result for this query
1000
1 - Follows While
while w;
Select BOOLEAN such that Follows(1, w)
we didn't write the result for this query
1000
1 - Follows While
while w;
Select BOOLEAN such that Follows(w, 1)
we didn't write the result for this query
1000
1 - Follows While
while w;
Select BOOLEAN such that Follows(w, w)
we didn't write the result for this query
1000
1 - Follows While
while w;
Select BOOLEAN such that Follows(w, _)
we didn't write the result for this query
1000
1 - Follows While
while w;
Select BOOLEAN such that Follows(_, w)
we didn't write the result for this query
1000
1 - Follows While
while w1, w2;
Select BOOLEAN such that Follows(w1, w2)
we didn't write the result for this query
1000
1 - Follows If
if i;
Select BOOLEAN such that Follows(1, i)
we didn't write the result for this query
1000
1 - Follows If
if i;
Select BOOLEAN such that Follows(i, 1)
we didn't write the result for this query
1000
1 - Follows If
if i;
Select BOOLEAN such that Follows(i, i)
we didn't write the result for this query
1000
1 - Follows While
if i;
Select BOOLEAN such that Follows(i, _)
we didn't write the result for this query
1000
1 - Follows While
if i;
Select BOOLEAN such that Follows(_, i)
we didn't write the result for this query
1000
1 - Follows If
if i1, i2;
Select BOOLEAN such that Follows(i1, i2)
we didn't write the result for this query
1000
1 - Follows Assign
assign a;
Select BOOLEAN such that Follows(1, a)
we didn't write the result for this query
1000
1 - Follows Assign
assign a;
Select BOOLEAN such that Follows(a, 1)
we didn't write the result for this query
1000
1 - Follows Assign
assign a;
Select BOOLEAN such that Follows(a, a)
we didn't write the result for this query
1000
1 - Follows Assign
assign a;
Select BOOLEAN such that Follows(a, _)
we didn't write the result for this query
1000
1 - Follows Assign
assign a;
Select BOOLEAN such that Follows(_, a)
we didn't write the result for this query
1000
1 - Follows Assign
assign a1, a2;
Select BOOLEAN such that Follows(a1, a2)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n;
Select BOOLEAN such that Follows(1, n)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n;
Select BOOLEAN such that Follows(n, 1)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n;
Select BOOLEAN such that Follows(n, n)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n;
Select BOOLEAN such that Follows(n, _)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n;
Select BOOLEAN such that Follows(_, n)
we didn't write the result for this query
1000
1 - Follows ProgLine
prog_line n1, n2;
Select BOOLEAN such that Follows(n1, n2)
we didn't write the result for this query
1000
