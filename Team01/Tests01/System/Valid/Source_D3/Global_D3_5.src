procedure vtuber {
    read ame;
    print ina;
    ina = ame + gura / cali % kiara - ina;
    if (kiara == calli) then {
        read ina;
        while (ina == 7) {
            calli = ame + 2;
            calli = gura + gura / (gura * gura) % gura;
            while (gura <= gura) {
                print gura;
                if (gura >= gura) then {
                    ame = kiara + kiara + gura;
                } else {
                    kiara = 1;
                    while (gura == ame) {
                        ame = ina;
                        read ame;
                        ame = kiara;
                        print ame;
                        ame = calli;
                        if (((ame != kiara) && (kiara == gura)) || (kiara != calli)) then {
                            ina = 7;
                            print ina;
                        } else {
                            ame = 4;
                            print kiara;
                        }
                        ame = 5;
                        print ame;
                    }
                    kiara = 1;
                    print calli;
                    kiara = calli / (calli * calli) + gura - ((kiara + ame) % ina);
                    read ame;
                }
                read ame;
                kiara = 4 + gura;
                print gura;
            }
            read gura;
            print gura;
            read ina;
        }
        print ina;
        read gura;
    } else {
        if (kiara < calli) then {
            if (ame > calli) then {
                while (kiara == (ame / (calli + ina))) {
                    print kiara;
                    kiara = ame + calli;
                    while(ina % calli != gura) {
                        print kiara;
                        read kiara;
                    }
                    print kiara;
                }
                while((calli >= calli) || (calli <= calli)) {
                    calli = calli;
                    calli = calli;
                }
                if (ina == (gura + calli)) then {
                    read ina;
                    read ina;
                } else {
                    print gura;
                    print gura;
                }
                print ame;
                read ame;
            } else {
                calli = gura;
                read ame;
            }
            ame = 5;
            if (ame != ina) then {
                ame = ina;
            } else {
                read ina;
            }
            while ((ame * (ina * gura)) <= (calli * kiara)) {
                read ame;
            }
            read ame;
        } else {
            print ina;
        }
        while (ina % 5 + 2 >= ame / 2) {
            ina = ame + calli;   
        }
        ina = 5;
    }
    ina = 5;
    read kiara;
}
