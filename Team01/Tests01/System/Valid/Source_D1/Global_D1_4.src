procedure main {
    a = 1;
    b = a;
    c = d;
    read a;
    if (a != 0) then {
        a = a + b + c + d + e;
        if (c == b) then {
            a = 5;
            print a;  
        } else {
            f = e / d + 5 * a;
            print f;
        }
    } else {
        while (a != b) {
            read c;
            c = a + b / 8;
            print c;
            while (a >= b) {
                read e;
                if (a < 0) then {
                    print e;
                } else {
                    c = a - e * 5;
                    if (c > e) then {
                        read a;
                        a = 1 * a + b / 2 * 4;
                        print d;
                    } else {
                        b = (1 * a + 2 * c + 3 * d + 4 * e) / 4;
                    }
                    a = b / 2 + 1;
                }
                print a;
                print b;
            }
        }
        print d;
    }
}
